[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "feishu-io"
version = "0.3.0"
description = "Minimal REST bridge for Feishu group markdown sending and unread message polling."
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[project.optional-dependencies]
server = [
    "fastapi>=0.111",
    "lark-oapi>=1.6.9",
    "pydantic-settings>=2.3",
    "uvicorn>=0.30",
]
test = [
    "fastapi>=0.111",
    "lark-oapi>=1.6.9",
    "pydantic-settings>=2.3",
    "uvicorn>=0.30",
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
]

[project.scripts]
feishu-io = "feishu_io.server_cli:main"
feishu-io-server = "feishu_io.server_cli:main"
feishu-io-listener = "feishu_io.server_cli:listener_main"
feishu-ioctl = "feishu_io.cli:main"

[tool.setuptools.packages.find]
include = ["feishu_io*"]
